<section class="page-header page-wrapper page-wrap">
    <header class="section__header page-header__header">
      <h1 class="section__title page-header__title">{{ page.title }}</h1>
    </header>
    <main class="section__main page-header__description">
      {{ page.content }}
    </main>
</section>
