<div class="margin"></div>
{% if restaurant.phone? %}
    <a class="menus-number" href="tel:{{ restaurant.phone_uri }}">
      {{ restaurant.phone }}
    </a>
{% endif %}

{% for menu in menus %}
<section class="index-header" id="menu-{{ menu.id }}">
  <header>
    <a href="{{ menu.url }}">
      <h2 class="index-header__title">{{ menu.name }}</h2>
    </a>
  </header>
  <ol class="menus-nav">
    {% for child in menu.children %}
      <li class="menus-nav__item">
      <a href="{{ menu.url }}#menu-{{child.id}}" class="menus-nav__link">
        {% if child.main_image? %}
          <div class="menus-nav__image" style="background-image: url('{{ child.main_image | small_image_url }}');background-size: cover;"></div>
        {% else %}
          <div class="menus-nav__image"></div>
        {% endif %}
        <h4 class="menus-nav__title">
          {{ child.name }}
        </h4>
      </a>
      </li>
    {% endfor %}
  </ol>
</section>
{% endfor %}
