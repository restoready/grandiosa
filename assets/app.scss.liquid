/*
* Grandiosa 1.0
* RestoReady v1.0
* Copyright 2014 MH Communication.
* Author MH Communication @MHCommunication
* Built with Sass - http://sass-lang.com/

* Some things to know about this file:
    - Sass is compiled on RestoReady's server so you don't need to convert it to CSS yourself
    - The output CSS is compressed and comments are removed
    - You cannot use @imports in this file
        * Use grunt or gulp tasks to enable @imports - https://github.com/Shopify/shopify-css-import
    - Helpers variables, mixins, and starter classes are provided. Change as needed.
    - The file is prepped with a CSS reset
*/
/* Table of Contents
==================================================
  Breakpoint and Grid Variables
  General Variables
  Sass Mixins
  Reset
  Grid Setup
  Helper Classes
  Images & Iframes
  Links and Buttons
  Color
  Proxy Color
  Fonts
  Proxy Fonts
  Style Full-visual theme -- RestoReady -- Défault mobile first
  Style Full-visual theme -- Breakpoint large - min 1024px
  Style Full-visual theme -- Breakpoint large - min 1350px
  Style Full-visual theme -- Breakpoint large - min 1500px
  Style Full-visual theme -- Breakpoint large - min 1730px
================================================== */
*, input, :before, :after {
  -webkit-box-sizing:border-box;
  -moz-box-sizing:border-box;
  box-sizing:border-box;
}
// ==============================================================================
// #Breakpoint and Grid Variables
// ==============================================================================
$viewportIncrement: 1px;

$small: 480px;
$small-up: 480px;
$medium: 1023px;
$large: 1024px;

$postSmall: $small + $viewportIncrement;
$preMedium: $medium - $viewportIncrement;
$preLarge: $large - $viewportIncrement;

/* The following are dependencies of csswizardry grid */
$breakpoints: (
  'small' '(max-width: #{$small})',
  'medium' '(min-width: #{$postSmall}) and (max-width: #{$medium})',
  'medium-down' '(max-width: #{$medium})',
  'large' '(min-width: #{$large})'
);
$breakpoint-has-widths: ('small', 'medium', 'medium-down', 'large');
$breakpoint-has-push:  ('medium', 'medium-down', 'large');
$breakpoint-has-pull:  ('medium', 'medium-down', 'large');

// ==============================================================================
// #General Variables
// ==============================================================================

/* Width variables */
$siteWidth: 1180px;
$gutter: 24px;
$gridGutter: 24px; // can be a %, but nested grids will have smaller margins because of it

/* Radius */
$radius: 3px;

/* Typography */
$headerFontStack: "HelveticaNeue", "Helvetica Neue", Helvetica, Arial, sans-serif;
$bodyFontStack: "HelveticaNeue", "Helvetica Neue", Helvetica, Arial, sans-serif;

$socialIconFontStack: "icons";


// ==============================================================================
// #Sass Mixins
// ==============================================================================
.clearfix {
  &:after {
    content: "";
    display: table;
    clear: both; }
  *zoom: 1; //hack IE6 et 7
}
@mixin clearfix() {
  &:after {
    content: "";
    display: table;
    clear: both; }
  *zoom: 1; //hack IE6 et 7
}

@mixin box-shadow($shadow) {
  -webkit-box-shadow: #{$shadow};
  -moz-box-shadow:    #{$shadow};
  box-shadow:         #{$shadow};
}

@mixin transition($transition:0.1s all) {
  -webkit-transition: #{$transition};
  -moz-transition: #{$transition};
  -o-transition: #{$transition};
  transition: #{$transition};
}

@mixin gradient($from, $to, $fallback) {
  background: $fallback;
  background: -moz-linear-gradient(top, $from 0%, $to 100%);
  background: -webkit-gradient(linear, left top, left bottom, color-stop(0%,$from), color-stop(100%,$to));
  background: -webkit-linear-gradient(top, $from 0%,$to 100%);
  background: -o-linear-gradient(top, $from 0%,$to 100%);
  background: -ms-linear-gradient(top, $from 0%,$to 100%);
  background: linear-gradient(top bottom, $from 0%,$to 100%);
}

@mixin backface($visibility: hidden) {
  -moz-backface-visibility: $visibility;
  -webkit-backface-visibility: $visibility;
  -o-backface-visibility: $visibility;
  backface-visibility: $visibility;
}

@function color-control($color) {
  @if (lightness( $color ) > 40) {
    @return #000;
  }
  @else {
    @return #fff;
  }
}

// ==============================================================================
// Dependency-free breakpoint mixin
// http://blog.grayghostvisuals.com/sass/sass-media-query-mixin/
// ==============================================================================
$min: min-width;
$max: max-width;
@mixin at-query ($constraint, $viewport1, $viewport2:null) {
  @if $constraint == $min {
    @media screen and ($min: $viewport1) {
      @content;
    }
  } @else if $constraint == $max {
    @media screen and ($max: $viewport1) {
      @content;
    }
  } @else {
    @media screen and ($min: $viewport1) and ($max: $viewport2) {
      @content;
    }
  }
}

// ==============================================================================
// #Reset
// ==============================================================================
html,body,div,span,applet,object,iframe,h1,h2,h3,h4,h5,h6,p,blockquote,pre,a,abbr,acronym,address,big,cite,code,del,dfn,em,img,ins,kbd,q,s,samp,small,strike,strong,sub,sup,tt,var,b,u,i,center,dl,dt,dd,ol,ul,li,fieldset,form,label,legend,table,caption,tbody,tfoot,thead,tr,th,td,article,aside,canvas,details,embed,figure,figcaption,footer,header,hgroup,menu,nav,output,ruby,section,summary,time,mark,audio,video{border:0;font-size:100%;font:inherit;vertical-align:baseline;margin:0;padding:0;}article,aside,details,figcaption,figure,footer,header,hgroup,menu,nav,section{display:block;}ol,ul{list-style:none;}blockquote,q{quotes:none;}blockquote:before,blockquote:after,q:before,q:after{content:none;}table{border-collapse:collapse;border-spacing:0;}code,pre{background-color:#faf7f5;font-family:Consolas,monospace;border:0 none;padding:0 2px;color:#51ab62;}pre{overflow:auto;padding:0.5em;margin:0 0 1em;}


// ==============================================================================
// #Grid Setup
//
// - Based on csswizardry grid, but with floated columns and a fixed gutter size
// - Breakpoints defined above, under #Breakpoint and Grid Variables
// - Note the inclusion of .grid-uniform to take care of clearfixes on evenly sized grid-items
// ==============================================================================
$responsive:         true;
$mobile-first:       true;
$use-silent-classes: false;
$push:               true;
$pull:               false;

$class-type: unquote(".");

@mixin grid-media-query($media-query) {
  $breakpoint-found: false;

  @each $breakpoint in $breakpoints {
    $name: nth($breakpoint, 1);
    $declaration: nth($breakpoint, 2);

    @if $media-query == $name and $declaration {
      $breakpoint-found: true;

      @media only screen and #{$declaration} {
        @content;
      }
    }
  }

  @if $breakpoint-found == false{
    @warn "Breakpoint '#{$media-query}' does not exist";
  }
}

/*------------------------------------*\
  $HELPERS
\*------------------------------------*/
/**
 * Create helper show/hide classes around our breakpoints.
 */
@mixin device-helper($namespace:"") {

  #{$class-type}#{$namespace}show        { display:block!important; }
  #{$class-type}#{$namespace}hide        { display:none!important; }

  #{$class-type}#{$namespace}text-left   { text-align:left!important; }
  #{$class-type}#{$namespace}text-right  { text-align:right!important; }
  #{$class-type}#{$namespace}text-center { text-align:center!important; }

  #{$class-type}#{$namespace}left        { float:left!important; }
  #{$class-type}#{$namespace}right       { float:right!important; }

}


/* Our regular, non-responsive width and helper classes. */
@include device-helper();

/* Our responsive classes, if we have enabled them. */
@if $responsive == true {

  @each $name in $breakpoint-has-widths {
    @include grid-media-query($name) {
      @include device-helper('#{$name}--');
    }
  }

}

// ==============================================================================
// #Helper Classes
// ==============================================================================
.left { float: left; }
.right { float: right; }
.text-center { text-align: center; }
.text-right { text-align: right; }
.hidden { display: none; }

.display-table {
  display: table;
}

.display-table-cell {
  display: table-cell;
  vertical-align: middle;
}

// ==============================================================================
// #Images and Iframes
// ==============================================================================
img.auto,
.grid-item img,
.grid-item iframe {
  max-width: 100%;
  height: auto;
}

img.circle {
  border-radius: 50%;
}

// ==============================================================================
// #Links and Buttons
// ==============================================================================

.btn {
  display: inline-block;
  padding: 8px 10px;
  margin: 0;
  line-height: 1.42;
  font-weight: bold;
  text-decoration: none;
  text-align: center;
  vertical-align: middle;
  white-space: nowrap;
  cursor: pointer;
  border: 1px solid transparent;
  -webkit-user-select: none;
  -moz-user-select:    none;
  -ms-user-select:     none;
  -o-user-select:      none;
  user-select:         none;
  -webkit-appearance: none;
  border-radius: $radius;

  &.btn--small {
    padding: 4px 5px;
    font-size: 0.75em; //12px
  }

  &.btn--full {
    width: 100%;
  }
}

// Force an input/button to look like a text link
.text-link {
  display: inline;
  border: 0 none;
  background: none;
  padding: 0;
  margin: 0;
}

// ==============================================================================
// #Color
// ==============================================================================
$red: #e62f2f;
$dark-grey: #1D1D1D;
$white: #EEEEEE;
$grey: #4A4A4A;
$soft-grey: #dddddd;
$grey: #606060;
$green: #3FB330;
$orange: #ED8A00;
$blue: #00B0D4;
// ==============================================================================
// #Proxy color
// ==============================================================================
$persoColor1: $dark-grey;
$persoColor2: $red;
$persoColor3: $white;
$border: $grey;
$back-label-vegan: $green;
$back-label-meat: $orange;
$back-label-porc: $blue;
$back-price-value: $soft-grey;
// ==============================================================================
// #Fonts
// ==============================================================================
$great: "Great Vibes";
$cabin: "Cabin";
// ==============================================================================
// #Proxy fonts
// ==============================================================================
$text: $cabin;
$slogan: $great;
// ==============================================================================
// #Style full-visual theme -- RestoReady -- Défault mobile first
// ==============================================================================
// =====================      #layout      ============================================
html, 
body {
  font-family: "{{ theme_settings.text_font | remove: 'Google+' | replace: '+', ' ' }}";
  height: 100%;
}
a {
  text-decoration: none;
  color: {{ theme_settings.perso-color-3 }};
}
h1,
span, 
tbody {
	color: {{ theme_settings.perso-color-3 }};
}
table, 
tr, 
td, 
th {
  border:0;
}
hr {
  border: solid 1px $persoColor1;
  width: 70%;
  margin-left: auto;
  margin-right: auto;
}
.page-header {
  background-color: #fff;
  padding: 1em 0;
}
    .page-header__title {
      color: #000;
      margin-bottom: 0.75rem;
    }
    .page-header__description {
      h2 {
        font-size: 1.2rem;
      }
      color: #000;
      p {
        margin-bottom: 0;
      }
    }
.section__title {
  color: #000;
  font-weight: 700;
  font-size: 1.5em;
}

.btn {
  background-color: {{ theme_settings.perso-color-2 }};
  color: {{ theme_settings.perso-color-3 }};
  margin-top: 50px;
  margin-bottom: 25px;
  width: 200px;
  height: 45px;
  font-size: 1.3em;
  transition: color 0.2s;

  &:hover {
    color: {{ theme_settings.perso-color-1 }};
    transition: color 0.2s;
  }
}
.facebook {
  height: 25px;
  width: 20px;
  background: url({{ 'fb.png' | asset_url }}) no-repeat center;
  display: inline-block;
  margin-bottom: 15px;
}
.twitter {
  height: 25px;
  width: 30px;
  background: url({{ 'twitt.png' | asset_url }}) no-repeat center;
  display: inline-block;
  margin-left: 15px;
  margin-bottom: 15px;
}
.tripadvisor {
  height: 25px;
  width: 42px;
  background: url({{ 'tripadvisor.png' | asset_url }}) no-repeat center;
  display: inline-block;
  margin-bottom: 15px;
  margin-left: 15px;
}
.back-to-menu {
  color: {{ theme_settings.perso-color-3 }};
  cursor: pointer;
}
.ico {
  width: 100%;
}
.back-to-menu-label {
  clear: both;
  text-align: center;
  width: 155px;
  margin-left: auto;
  margin-right: auto;
  transition: color 0.2s;
  font-size: 0.75em;
  color: {{ theme_settings.perso-color-1 }};
}
.arrow {
  width: 0;
  height: 0;
  border-style: solid;
  border-width: 0 10px 10px 10px;
  border-color: transparent transparent {{ theme_settings.perso-color-1 }} transparent;
  display: inline-block;
  margin: 0 10px;
}
.return {
  margin-top: 10px;
}
// ===================== #content styles -- front.liquid ============================================
.footer__infos {
  width: 100%;
  background-color: {{ theme_settings.perso-color-1 }};
  clear: both;
  padding-top: 20px;
}
// ===================== #nav styles -- _nav.liquid ============================================
.nav-small {
	position: fixed;
  top: 0;
  z-index: 9999;
	width: 100%;
	background-color: {{ theme_settings.perso-color-1 }};
}
.nav-small__logo {
	padding-top: 5px;
  padding-bottom: 5px;
  display: inline-block;
  img {
    width: 60px;
  }
}
.nav-small__btn {
  display: inline-block;
  right: 30px;
  margin-top: 10px;
  position: absolute;
  cursor: pointer;
  z-index: 9999;
  p {
    color: white;
  }
  div {
    width: 40px;
    height: 5px;
    margin-top: 5px;
    background-color: #FFF;
  }
}
.nav-small__name-resto {
	width: 100%;
  margin-left: 75px;
  top: 16px;
	font-size: 1.2em;
  padding-bottom: 15px;
  position: absolute;
}

.sub-nav__items {
  padding: 0;
  display: none;
  opacity: 0;
  visibility: hidden;
  margin: 0;
  background-color: {{ theme_settings.perso-color-1 }};
}
.sub-nav__item {
  display: block;
  width: 100%;
  padding: 0 0 0 10px;
  background-color: {{ theme_settings.perso-color-1 }};
}
.sub-nav__link{
  display: inline-block;
  color: {{ theme_settings.perso-color-3 }};
  text-decoration: none;
  width: 100%;
  padding-left: 25px;
  padding-top: 10px;
  padding-bottom: 10px;
  transition: color .25s;
  &:hover {
    color: {{ theme_settings.perso-color-2 }};
  }
}
.nav-large__item:hover .sub-nav__items,
.nav-small__item:hover .sub-nav__items {
  display: block;
  opacity: 1;
  visibility: visible;
}

.sb-slidebar {
  margin: 0;
  padding: 0;
  margin-top: 50px;
  -webkit-box-sizing: border-box;
  -moz-box-sizing: border-box;
  box-sizing: border-box;
  height: 100%;
  overflow-y: auto; /* Enable vertical scrolling on Slidebars when needed. */
  position: fixed;
  width: 250px;
  display: none;
  z-index: 99; /* Slidebars sit behind sb-site. */
  background-color: {{ theme_settings.perso-color-1 }}; /* Default Slidebars background colour, overwrite this with your own css. */
}
.sb-right {
  right: 0; /* Set Slidebar to the right. */
  top: 0;
}
.nav-small__list, 
.nav-large__list {
  text-align: left;
  list-style: none;
  margin-top: 40px;
  margin-left: 0;
  border-bottom: solid 2px {{ theme_settings.border }};
}
.nav-small__item, 
.nav-large__item {
    cursor: pointer;
    font-size: 1.2em;
    border-top: solid 2px {{ theme_settings.border }};
    transition: background-color 0.5s;
    font-weight: 200;
    width: 100%;
    background-color: {{ theme_settings.perso-color-1 }};
    &:hover {
      background-color: {{ theme_settings.perso-color-2 }};
    }
}
.nav-small__item {
  width: 250px;
}
.nav-small__item-links, 
.nav-large__item-links {
  display: inline-block;
  color: {{ theme_settings.perso-color-3 }};
  text-decoration: none;
  width: 100%;
  padding-left: 25px;
  padding-top: 10px;
  padding-bottom: 10px;
}
.nav-large__coordinate {
  padding-bottom: 15px;
  margin-top: 25px;
  width: 200px;
  font-size: 1em;
  font-family: "{{ theme_settings.text_font | remove: 'Google+' | replace: '+', ' ' }}";
}
.nav-small__coordinate {
  padding-bottom: 15px;
  margin-top: 25px;
  width: 200px;
  font-size: 1em;
  margin-left: 25px;
  text-align: center;
  font-family: "{{ theme_settings.text_font | remove: 'Google+' | replace: '+', ' ' }}";
}
.nav-small__coordinate-number {
  color: {{ theme_settings.perso-color-2 }};
}
// ===================== #menu styles -- _index.liquid ============================================
.index-header {
  clear: both;
}
.index-header__title {
  color: {{ theme_settings.perso-color-1 }};
  font-size: 2em;
  font-weight: 700;
  padding-top: 30px;
  margin-left: 15px;
}
// ===================== #menu styles -- _show.liquid ============================================
.menus-number {
  display: block;
  font-size: 2em;
  line-height: 1;
  width: 220px;
  text-align: center;
  margin-left: auto;
  margin-right: auto;
  padding-top: 15px;
  padding-bottom: 15px;
  font-family: "{{ theme_settings.text_font | remove: 'Google+' | replace: '+', ' ' }}";
  transition: color 0.2s;
  color: {{ theme_settings.perso-color-1 }};
  &:hover {
    color: {{ theme_settings.perso-color-2 }};
    transition: color 0.2s;
  }
}
.menus-nav {
  width: 100%;
  position: relative;
  clear: both;
  overflow: hidden;
}
.menus-nav__item {
  width: 50%;
  float: left;
  background-color: {{ theme_settings.perso-color-1 }};
}
.menus-nav__link {
  display: block;
  position: relative;
  cursor: pointer;
  opacity: 0.75;
  transition: opacity 0.2s;
  -webkit-transform: translateZ(0);
  &:hover {
    opacity: 1;
  }
}
.menus-nav__image {
  width: 100%;
  display: block;
  position: relative;
} 
.menus-nav__title {
  color: {{ theme_settings.perso-color-3 }};
  width: auto;
  background-color: {{ theme_settings.perso-color-1 }};
  opacity: 0.9;
  position: absolute;
  bottom: 0;
  -webkit-transform: translateY(-50%);
  left: 0;
  font-family: "{{ theme_settings.text_font | remove: 'Google+' | replace: '+', ' ' }}";
  font-size: 1.5em;
  padding: 5px 5px;
}
// ===================== #menus-food styles -- _menu.liquid ============================================
.menus-food {
  clear: both;
  width: 100%;
  margin-top: 50px;
}
.menus-food__head--featured {
  background-color: {{ theme_settings.perso-color-1 }};
  color: {{ theme_settings.perso-color-3 }};
  display: inline-block;
  padding: 5px 10px 5px 10px;
  border-radius: 3px 3px 0 0;
}
.head-menu {
  display: inline-block;
}
.menus-food__content {
  max-width: 85%;
  margin-left: auto;
  margin-right: auto;
  position: relative;
  padding-bottom: 15px;
}
.margin {
  height: 50px;
}
.content-menus-food__title-1 {
  font-size: 2em;
  font-weight: 700;
  display: inline-block;
}
.menu-food__title2 {
  width: auto;
}
.menus-food__featured {
  display: inline-block;
}
.content-menus-food__title-2 {
  font-size: 1.5em;
  color: {{ theme_settings.perso-color-1 }};
  padding: 5px 5px 5px 5px;
  border-radius: 2px;
  display: inline;
}
.under-title-1 {
  display: none;
}
.under-title-2 {
  width: 100%;
  margin-left: 0;
}
.content-menus-food__description-title-1 {
  font-size: 1em;
  color: {{ theme_settings.perso-color-1 }};
  font-style: italic;
}
.content-menus-food__description-title-2 {
  margin-top: 15px;
}
.content-menus-food__description--featured {
  color: {{ theme_settings.perso-color-3 }};
}
.menus-footer {
  width: 100%;
  margin-top: 25px;
}
.block-grid {
  overflow: hidden;
}
.block-grid--featured {
  border: 6px solid {{ theme_settings.perso-color-1 }};
  border-radius: 0 3px 3px 3px;
  padding: 0 10px 5px 20px;
  margin-right: 5px;
}
// ===================== #food styles -- _food.liquid ============================================
.dish {
  margin-top: 25px;
  padding-left: 0px;
}
.dish__name {
  font-weight: 700;
  margin-bottom: 10px;
  font-size: 1.2em;
}
.dish__name--featured {
  color: {{ theme_settings.perso-color-3 }};
  display: inline-block;
  background-color: {{ theme_settings.perso-color-1 }};
  padding: 5px 10px 5px 10px;
  border-radius: 3px;
  margin-top: 5px;
  margin-bottom: 0px;
}
.dish__image {
  max-width: 100%;
}
.dish__description-summary {
  font-weight: 500;
}
.dish__description-label {
  margin-bottom: 15px;
  color: {{ theme_settings.perso-color-3 }};
  margin-top: 15px;
}
.label-new {
  color: {{ theme_settings.perso-color-1 }};
  border: 3px solid {{ theme_settings.perso-color-2 }};
  padding: 5px 5px 5px 5px;
  border-radius: 2px;
  text-align: center;
}
.label-vegan {
  color: {{ theme_settings.perso-color-1 }};
  border: 3px solid {{ theme_settings.back-label-vegan }};
  padding: 5px 5px 5px 5px;
  border-radius: 2px;
  text-align: center;
}
.label-no-meat {
  color: {{ theme_settings.perso-color-1 }};
  border: 3px solid {{ theme_settings.back-label-meat }};
  padding: 5px 5px 5px 5px;
  border-radius: 2px;
  text-align: center;
}
.label-no-pork {
  color: {{ theme_settings.perso-color-1 }};
  border: 3px solid {{ theme_settings.back-label-pork }};
  padding: 5px 5px 5px 5px;
  border-radius: 2px;
  text-align: center;
}
.food-price {
  text-align: right;
}
.food-price__label, 
.food-price__value {
  font-family: "{{ theme_settings.text_font | remove: 'Google+' | replace: '+', ' ' }}";
  font-weight: 500;
  font-style: italic;
  color: {{ theme_settings.perso-color-1 }};
  margin-top: 10px;
}
.food-price__value {
  background-color: {{ theme_settings.back-price-value }};
  border-radius: 3px;
  padding: 3px 3px;
}
// ===================== #terms styles -- _terms.liquid ============================================
.terms-content {
  max-width: 85%;
  margin-left: auto;
  margin-right: auto;
  margin-top: 75px;
  margin-bottom: 50px;

  a {
    color: {{ theme_settings.perso-color-2 }};
  }
}
.terms-title {
  font-size: 2em;
  font-weight: 700;
  margin-bottom: 25px;
}
.terms-title-level-1 {
  font-size: 1.4em;
  font-style: italic;
  font-weight: 500;
}
.terms-title-level-2 {
  font-size: 1.1em;
}
// ===================== #pages -- page.liquid ============================================================
.page-wrapper {
  max-width: 85%;
  margin-left: auto;
  margin-right: auto;
  margin-top: 50px;
  overflow: hidden;
}
// ===================== #404 -- 404.liquid ============================================================
.page-error {
  max-width: 85%;
  margin-left: auto;
  margin-right: auto;
  margin-top: 50px;
}
.ribbon-content {
  color: {{ theme_settings.perso-color-2 }};
}
// ===================== #resto styles -- tenants _show.liquid ============================================
.resto-presentation {
  width: 100%;
}
.resto-presentation__image-wrapper {
  background-color: #000;
}
.resto-presentation__image {
  {% if theme_settings.use_restaurant_image %}
    background: {{ theme_settings.perso-color-3 }} url({{ 'restaurant.jpg' | asset_url }}) no-repeat center;
  {% else %}
    background-color: {{ theme_settings.perso-color-3 }};
  {% endif %}
  background-size: cover;
  height: 200px;
  opacity: 0.7;
}
.resto-presentation__content {
  max-width: 85%;
  margin-left: auto;
  margin-right: auto;
  margin-top: 30px;
}
.resto-presentation__title {
  font-size: 2em;
  font-weight: 700;
  margin-bottom: 25px;
}
.resto-presentation__description {
  margin-bottom: 25px;
}
.gallery {
  max-width: 100%;
  margin-left: auto;
  margin-right: auto;
  margin-bottom: 40px;
}

// ===================== #photos styles -- _photo.liquid ============================================
.gallery__item {
    position: relative;
    margin-bottom: 1em;
    margin-top: 25px;
}
.gallery-item__image {
  max-width: 100%;
  height: auto;
}
.gallery-item__caption {
  font-size: 0.85em;
  font-weight: 700;
  font-style: italic;
}
// ===================== #subscribe-page styles -- subscibers _new.liquid & _thanks.liquid ============================================
.subscribe-page {
  padding-top: 75px;
  max-width: 85%;
  margin-left: auto;
  margin-right: auto;

  input {
    display: block;
    width: 300px;
    height: 35px;
    margin-top: 25px;
  }
}
.submit-subscriber {
  margin-top: 0px;
}
.subscriber-error {
  color: {{ theme_settings.perso-color-2 }};
  font-weight: 700;
  text-transform: uppercase;
}
.subscribe-page__title {
  color: {{ theme_settings.perso-color-1 }};
  font-size: 2em;
  font-weight: 700;
}
.subscribe-page__description {
  padding-bottom: 25px;
  margin-top: 15px;
}
.newsletter-description-oups {
  font-size: 1.5em;
  font-weight: 700;
}
.newsletter-description-error {
  border-left: 5px solid {{ theme_settings.perso-color-2 }};
  padding-left: 5px;
}
// ===================== #contact-page styles -- message _new.liquid & _thanks.liquid ============================================
.contact-page {
  padding-top: 75px;
  max-width: 85%;
  margin-left: auto;
  margin-right: auto;

  input, textarea {
    display: block;
    width: 100%;
    height: 35px;
    margin-top: 10px;
  }
  textarea {
    height: 250px;
  }
  label {
    display: block;
    margin-top: 25px;
    font-style: italic;
    font-weight: 500;
  }
}
.contact-page__title {
  color: {{ theme_settings.perso-color-1 }};
  font-size: 2em;
  font-weight: 700;
}
.contact-page__description {
  padding-bottom: 25px;
  margin-top: 15px;
}
.submit-contact {
  margin-top: 20px;
  float: right;
  width: 150px;
}
// ===================== #footer styles -- _footer.liquid ============================================
.footer,
.contact-footer {
  color: {{ theme_settings.perso-color-3 }};
  font-family: "{{ theme_settings.text_font | remove: 'Google+' | replace: '+', ' ' }}";
}
.footer {
  text-align: center;
  margin-top: -30px;
  width: 250px;
  margin-left: auto;
  margin-right: auto;
  padding-top: 15px;
}
.social-footer {
  margin-top: 0px;
}
.social-footer__item {
  display: inline-block;
}
.contact-footer__name, 
.socail-footer__title {
  margin-bottom: 15px;
  font-size: 2em;
  font-weight: 500;
}
.contact-footer__street-address, 
.contact-footer__postal-code, 
.contact-footer__address-locality, 
.contact-footer__phone, 
.contact-footer__mail {
  font-weight: 500;
}
.contact-footer__mentions {
  color: mix($persoColor3, $persoColor1, 50%);
}
.contact-footer__phone-link, 
.contact-footer__mail-link {
  text-decoration: none;
}
.subscribe__title {
  color: {{ theme_settings.perso-color-3 }};
  font-style: italic;
  margin-top: 25px;
}
.subscribers-form {
  margin-top: -10px;
}
.subscribe__email-subscriber {
  width: 70%;
  height: 30px;
}
.subscribe__submit-subscriber {
  margin-top: 15px;
  width: 70%;
}
.footer-link {
  margin-bottom: 15px;
  display: inline-block;
  padding-bottom: 10px;
}
// ===================== Background #img styles -- home.liquid ============================================
.full-background {
	width: 100%;
  height: auto;
  margin-top: 53px;
  display: inline-block;
  background-color: #000;
  position: relative;
}
.full-background__image {
  height: 225px;
  {% if theme_settings.use_full_home_image %}
    background: {{ theme_settings.perso-color-1 }} url({{ 'full-home.jpg' | asset_url }}) no-repeat center;
  {% else %}
    background-color: {{ theme_settings.perso-color-1 }};
  {% endif %}
  background-size: cover;
  opacity: 0.6;
}
.slogan-wrapper {
  width: 100%;
  position: absolute;
  top: 7%;
  text-align: center;
}
.slogan {
	font-size: 2.5em;
	line-height: 1;
	max-width: 80%;
	text-align: center;
	margin-left: auto;
	margin-right: auto;
	font-family: "{{ theme_settings.slogan_font | remove: 'Google+' | replace: '+', ' ' }}";
  color: {{ theme_settings.perso-color-3 }};
  padding-top: 25px;
  padding-bottom: 25px;
}
.home-number {
  display: block;
  font-size: 2em;
  line-height: 1;
  width: 220px;
  text-align: center;
  margin-left: auto;
  margin-right: auto;
  padding-top: 10px;
  padding-bottom: 25px;
  font-family: "{{ theme_settings.text_font | remove: 'Google+' | replace: '+', ' ' }}";
  transition: color 0.2s;
  color: {{ theme_settings.perso-color-3 }};
  &:hover {
    color: {{ theme_settings.perso-color-2 }};
    transition: color 0.2s;
  }
}
// ===================== Section #week & #new-dish styles -- home.liquid ============================================
.news {
  width: 100%;
}
.week,
.new-dish {
  margin-top: 25px;
  max-width: 85%;
  margin-left: auto;
  margin-right: auto;
}
.week__title, 
.new-dish__title, 
.home-description__title, 
.hours__title {
	font-size: 2em;
	font-weight: 700;
  margin-top: 15px;
}
.week__resume, 
.new-dish__resume {
	font-size: 1.2em;
	font-weight: 500;
	font-style: italic;
  margin-top: 25px;
}
.week__details, 
.new-dish__details {
	font-size: 1em;
	font-weight: 200;
	font-style: italic;
}
.news-footer, 
.presentation-footer {
  max-width: 100%;
  padding-bottom: 1px;
}
.news-footer__btn {
  width: 200px;
  display: block;
  margin-left: auto;
  margin-right: auto;
}
// ===================== Section #presentation styles -- home.liquid ============================================
.presentation {
  width: 100%;
  height: auto;
  {% if theme_settings.use_presentation_image %}
    background: {{ theme_settings.perso-color-2 }} url({{ 'presentation.jpg' | asset_url }}) no-repeat center;
  {% else %}
    background-color: {{ theme_settings.perso-color-2 }};
  {% endif %}
  background-size: cover;
}
.home-description {
  max-width: 85%;
  margin-left: auto;
  margin-right: auto;
}
.home-description__summary {
  margin-top: 25px;
  color: {{ theme_settings.perso-color-3 }};
}
.home-description__title, 
.hours__title {
  color: {{ theme_settings.perso-color-3 }};
  padding-top: 25px;
}
.presentation-footer__btn {
  width: 200px;
  display: block;
  margin-left: auto;
  margin-right: auto;
}
// ===================== Section #horaires styles -- home.liquid ============================================
.opening-hours {
  width: 100%;
  height: auto;
  {% if theme_settings.use_horaires_image %}
    background: #e62f2f url({{ 'horaires.png' | asset_url }}) no-repeat center;
  {% else %}
    background-color: {{ theme_settings.perso-color-3 }};
  {% endif %}
  background-size: cover;
  margin-top: -15px;
}
.hours {
  max-width: 85%;
  margin-left: auto;
  margin-right: auto;
}
.hours__title {
  margin-bottom: 25px;
}
.hours__information {
  color: {{ theme_settings.perso-color-3 }};
  margin-top: 25px;
  font-weight: 500;
  padding-bottom: 25px;
}
.hours__table {
  font-family: "{{ theme_settings.text_font | remove: 'Google+' | replace: '+', ' ' }}";
}
.hours__day {
  line-height: 3.5;
}
.hours__day-name {
  font-size: 1.3em;
  text-align: left;
}
.hours__day-shifts {
  font-size: 1.3em;
  padding-left: 25px;
  line-height: 1.2;
  font-style: italic;
}
// ===================== Section #map styles -- home.liquid ============================================
.directions, 
.directions__map-link {
  width: 100%;
  position: relative;
}
.directions__map-image {
  width: 100%;
}
.infos-local__address {
  color: {{ theme_settings.perso-color-1 }};
  font-size: 1em;
  font-weight: 500;
}
.directions-footer {
  width: 100%;
  bottom: 0;
  position: absolute;
}
.directions-footer__btn {
  width: 200px;
  display: block;
  margin-left: auto;
  margin-right: auto;
}
// ===================== Section #page styles -- page.liquid ============================================
.page-header {
  padding: 1em 0 0 0;
}
.page-header__header {
  font-size: 3rem;
  h1 {
    color: {{ theme_settings.perso-color-2 }};
  }
}
.page-body {
    margin-bottom: 3rem;
    margin-right: 0.5rem;
    ol, ul, dl {
      margin: 1rem 1rem 1rem 2rem;
    }
    ol {
      list-style: decimal;
    }
    ul {
      list-style: disc;
    }
    img {
      max-width: 100% !important;
      height: auto !important;
    }
    p {
      margin-bottom: 0.5rem;
    }
    a {
      color: {{ theme_settings.perso-color-2 }};
      text-decoration: none;
      &:hover, &:focus {
        color: #999;
      }
    }
    h1 { font-size: 3em; }     
    h2 { font-size: 2.35em; } 
    h3 { font-size: 2em; }      
    h4 { font-size: 1.429em; }  
    h5 { font-size: 1.143em; }  
    h6 { font-size: 1em; }    
  }
// ==============================================================================
// #Style full-visual theme -- Breakpoint small-up - min 480px
// ==============================================================================
@media only screen and (min-width: $small-up) {
  // ===================== #food styles -- _food.liquid ============================================
  .dish {
    padding-right: 25px;
    width: 50%;
    height: auto;
    display: block;
    float: left;
  }
  .label-new,
  .label-vegan,
  .label-no-meat,
  .label-no-porc {
    display: block;
    margin-top: 3px;
  }
  // ===================== #menu styles -- _menu.liquid ============================================
  .block-grid {
    &>li {
      &:nth-of-type(2n+1) {
        clear: left;
      }
    }
  }
  .block-grid--featured {
    padding-right: 0px;
    margin-right: 25px;
  }
  .under-title-2 {
    width: 95%;
  } 
  // ===================== #menu styles -- _show.liquid ============================================
  .menus-nav__item {
    width: 33.3333333%;
  }
  .page-wrap {
    min-height: 100%;
    /* equal to footer height */
    margin-bottom: -200px; 
  }
  .page-wrap:after {
    content: "";
    display: block;
    /* .push must be the same height as footer */
    height: 175px; 
  }
  // ===================== #week & dish styles -- _home.liquid ============================================
  .news-footer {
    max-width: 85%;
    margin-left: auto;
    margin-right: auto;
  }
  .news-footer__btn {
    margin-left: 0;
  }
  // ===================== Section #presentation styles -- home.liquid ============================================
  .presentation-footer {
    max-width: 85%;
    margin-left: auto;
    margin-right: auto;
  }
  .presentation-footer__btn {
    margin-left: 0;
  }
  // ===================== Section #photo styles -- photo.liquid ============================================
  .gallery__item {
    display: inline-block;
    vertical-align: top;
    width: 45%;
    margin: 2%;
  }
}
// ==============================================================================
// #Style full-visual theme -- Breakpoint small-up - min 650px
// ==============================================================================
@media only screen and (min-width: 650px) {
  // ===================== #layout ============================================
  hr {
    display: none;
  }
  .under-title-2 {
    display: block;
    width: 97%;
  }
  // ===================== #footer styles -- _footer.liquid ============================================
  .footer {
    width: 100%;
  }
  .contact-footer,
  .newsletter-footer {
    display: inline-block;
    width: 45%;
    vertical-align: top;
    margin-top: 15px;
  }
  .footer-link {
    display: block;
    margin-bottom: 0px;
    text-align: center;
  }
  .subscribe__title {
    margin-top: 10px;
  }
}
// ==============================================================================
// #Style full-visual theme -- Breakpoint large - min 1024px
// ==============================================================================
@media only screen and (min-width: $large) {
// ===================== #content styles -- front.liquid ============================================
.footer__infos {
  /* .push must be the same height as footer */
  height: 200px;
}
// ===================== #nav styles -- _nav.liquid ============================================
  .nav-large {
  	width: 300px;
  	height: 100%;
  	background-color: {{ theme_settings.perso-color-1 }};
  	position: fixed;
  	display: inline-block;
  	margin-left: 75px;
  	text-align: center;
    top: 0;
    z-index: 9999;
  }
  .nav-large__logo {
    padding-top: 15px;
    img {
      width: 200px;
    }
  }
  .nav-large__name-resto {
  	width: 100%;
  	font-size: 1.5em;
  	margin-top: 15px;
  	font-weight: 300;
  }
  .sub-nav__link{
    display: inline-block;
    color: {{ theme_settings.perso-color-3 }};
    text-decoration: none;
    width: 100%;
    padding-left: 25px;
    padding-top: 10px;
    padding-bottom: 10px;
  }
  .social-nav__item {
    display: inline-block;
  } 
  .nav-large__coordinate {
  	width: 300px;
    margin-top: 50px;
  }
  .nav-large__coordinate-number, 
  .nav-large__coordinate-address {
  	font-size: 1em;
  	font-weight: 200;
  }
  .nav-large__coordinate-number {
  	font-size: 1.4em;
    color: {{ theme_settings.perso-color-2 }};
  }
// ===================== #menu styles -- _index.liquid ============================================
  .index-header__title {
    margin-left: 400px;
    margin-top: 0px;
    padding-top: 30px;
  }
// ===================== #menu styles -- _show.liquid ============================================
  .menus-number {
    display: none;
  }
  .menus-nav {
    margin-top: 0px;
    width: 60%;
    margin-left: 375px;
  }
  .menus-nav__item {
    width: 33.3333333%;
  }
  // ===================== #food styles -- _food.liquid ============================================
  .dish {
    padding-right: 25px;
    width: 33.3333333%;
    display: block;
    float: left;
    height: auto;
  }
// ===================== #menus-food styles -- _menu.liquid ============================================
  .menus-food {
    width: auto;
  }
  .menus-food__content {
    margin-left: 400px;
  }
  .margin {
    height: 0px;
  }
  .block-grid {
    &>li {
      &:nth-of-type(2n+1) {
        clear: none;
      }
      &:nth-of-type(3n+1) {
        clear: left;
      }
    }
  }
  .under-title-2 {
    width: 63%;
  }
// ===================== #pages -- page.liquid ============================================================
  .page-wrapper {
    margin-left: 400px;
    margin-right: 0;
    margin-top: 0;
  }
// ===================== #404 -- 404.liquid ============================================================
.page-error {
  margin-left: 400px;
  margin-right: 0;
  margin-top: 0;
}
// ===================== #resto styles -- tenants _show.liquid ============================================
  .resto-presentation__content {
    margin-left: 400px;
    margin-right: 0;
    margin-top: 25px;
  }
  .resto-presentation__image {
    height: 500px;
  }
  .gallery {
    max-width: 100%;
    margin-left: auto;
    margin-right: auto;
  }
// ===================== #terms styles -- _terms.liquid ============================================
  .terms-content {
    margin-left: 400px;
    margin-top: 25px;
  }
// ===================== Background #img styles -- home.liquid ============================================
  .full-background {
    height: 100%;
    margin-top: 0px;
  }
  .full-background__image {
    height: 100%;
  }
  .slogan {
  	font-size: 3em;
  	margin-left: 400px;
  	padding-top: 25%;
  }
  .home-number {
    display: none;
  }
// ===================== Section #week & #new-dish styles -- home.liquid ============================================
  .news {
    width: auto;
  }
  .week,
  .new-dish {
    margin-left: 400px;
    margin-right: 0px;
  }
  .home-description, 
  .hours {
  	margin-left: 400px;
  }
  .news-footer, 
  .presentation-footer, 
  .directions-footer {
    margin-left: 400px;  
  }
// ===================== #map styles -- home.liquid ============================================
  .infos-local {
  max-width: 65%;
  margin-left: 400px;
  display: block;
  bottom: 15%;
 }
 .directions-footer {
  max-width: 50%;
 }
 .directions-footer__btn {
  margin-left: 0;
}
// ===================== #footer styles -- _footer.liquid ============================================
  .subscribe {
    padding-top: 25px;
  }
  .subscribe__email-subscriber {
    width: 300px;
    height: 35px;
    float: left;
    margin: 5px 25px 0 0;
  }
  .footer {
    margin-top: -30px;
    text-align: left;
    margin-left: 400px;
    max-width: 55%;
  }
  .newsletter-footer {
    margin-top: 0px;
    width: 100%;
  }
  .footer-link {
    text-align: left;
  }
  .subscribe__submit-subscriber {
    width: 200px;
    margin-top: 0;
  }
  // ===================== #subscribe-page styles -- subscribers _thanks.liquid ============================================
 .subscribe-page {
    margin-left: 400px;
    padding-top: 25px;
  }
  // ===================== #contact-page styles -- message _new.liquid & _thanks.liquid ============================================
  .contact-page {
    margin-left: 400px;
    padding-top: 25px;
    margin-right: 25px;
    max-width: 60%;
  }
  // ===================== Section #photo styles -- photo.liquid ============================================
  .gallery__item {
    width: 30%;
    margin: 1%;
  }
}
// ==============================================================================
// #Style full-visual theme -- Breakpoint - min 1200px
// ==============================================================================
@media only screen and (min-width: 1200px) {
  // ===================== #menu styles -- _show.liquid ============================================
  .menus-nav {
    width: 66%;
  }
  .menus-nav__item {
    width: 33.3333333%;
  }
  .under-title-2 {
    width: 64%;
  }
  // ===================== #food styles -- _food.liquid ============================================
  .label-new,
  .label-vegan,
  .label-no-meat,
  .label-no-porc {
    display: inline;
    margin-top: 0px;
  }
}
// ==============================================================================
// #Style full-visual theme -- Breakpoint - min 1350px
// ==============================================================================
@media only screen and (min-width: 1350px) {
// ===================== Background #img styles -- home.liquid ============================================
  .slogan {
    font-size: 3.5em;
    padding-top: 20%;
  }
  // ===================== #menu styles -- _show.liquid ============================================
  .menus-nav {
    width: 70%;
  }
  .menus-nav__item {
    width: 25%;
  }
  // ===================== #map styles -- home.liquid ============================================
  .infos-local {
    max-width: 75%;
    margin-left: 400px;
    bottom: 12%;
  }
}
// ==============================================================================
// #Style full-visual theme -- Breakpoint - min 1500px
// ==============================================================================
@media only screen and (min-width: 1500px) {
// ===================== Background #img styles -- home.liquid ============================================
  .slogan {
    font-size: 3.5em;
  }
  // ===================== #menu styles -- _show.liquid ============================================
  .menus-nav {
    width: 72%;
  }
  .menus-nav__item {
    width: 25%;
  }
  // ===================== #map styles -- home.liquid ============================================
  .infos-local {
    bottom: 10%;
  }
}
// ==============================================================================
// #Style full-visual theme -- Breakpoint - min 1730px
// ==============================================================================
@media only screen and (min-width: 1730px) {
// ===================== Background #img styles -- home.liquid ============================================
  .slogan  {
	   font-size: 5em;
  }
  // ===================== #menu styles -- _show.liquid ============================================
  .menus-nav {
    width: 77%;
  }
  .menus-nav__item {
    width: 25%;
  }
  .under-title-2 {
    width: 65%;
  }
  // ===================== #map styles -- home.liquid ============================================
  .infos-local {
    max-width: 80%;
    margin-left: 400px;
  }
}
// ==============================================================================
// #Style full-visual theme -- Breakpoint - min 1870px
// ==============================================================================
@media only screen and (min-width: 1870px) {
  // ===================== #food styles -- _food.liquid ============================================
  .food-price {
    padding-right: 20px;
  }
}
